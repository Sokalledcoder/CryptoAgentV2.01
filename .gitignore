# Environment variables
.env
*.env
.env.*
env/
venv/
.venv/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
*.so
*.pyc

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Usually committed, but can be ignored if yarn.lock or pnpm-lock.yaml is used and preferred
# If you are using yarn, uncomment the next line and comment out package-lock.json if desired
# yarn.lock
# If you are using pnpm, uncomment the next line and comment out package-lock.json if desired
# pnpm-lock.yaml
dist/ # Common for build output in Node.js projects too
.next/ # Next.js build output
*.tsbuildinfo # TypeScript build info

# IDE / Editor specific
.vscode/
.idea/
*.swp
*.swo

# OS specific
.DS_Store
Thumbs.db

# Log files
*.log

# Workspaces (if they are generated and shouldn't be versioned)
# workspaces/* # Be careful with this, only if workspaces are truly ephemeral.
# If specific workspace outputs should be ignored:
# workspaces/*/some_generated_file.json

# CopilotKit specific (if any cache or local files are generated)
# .copilotkit/ # Example, if such a directory is created

# Other
coverage/
.nyc_output/
